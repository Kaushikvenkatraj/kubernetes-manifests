apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: apps-routing
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/Kaushikvenkatraj/kubernetes-manifests
    targetRevision: HEAD
    path: apps

  destination:
    server: https://kubernetes.default.svc
    namespace: default

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
